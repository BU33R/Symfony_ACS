{% extends 'base.html.twig' %}

{% block body %}

<article>

    <h2>{{ project.title }}</h2>

    <div class="content">

        <img src="{{ project.image }}" alt="">
        
        {{ project.description | raw }}

    </div>

</article>

{% endblock %}